on: [pull_request]

jobs:
  test-github-action:
    runs-on: ubuntu-latest
    name: Test GitHub Action
    steps:
      - name: Test
        uses: john-d-pelingo/jira-link-issue-action@v0.0.2
        with:
          atlassian-domain: 'https://johndpelingo.atlassian.net'
          board-name: 'MEME'
