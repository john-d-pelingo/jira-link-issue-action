on: [pull_request]

jobs:
  test-github-action:
    runs-on: ubuntu-latest
    name: Test GitHub Action
    steps:
      - name: Test
        uses: ./
        with:
          atlassian-domain: 'https://johndpelingo.atlassian.net'
          board-name: 'MEME'
